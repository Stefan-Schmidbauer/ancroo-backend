# Internal — Archived Files

This directory contains files that were removed from the active codebase but kept for potential future use.

## standalone/

Standalone deployment files (Docker Compose + env template) for running Ancroo Backend without Ancroo Stack. Removed to reduce complexity during Phase 1 — the only supported deployment method is via Ancroo Stack.

To restore: move `standalone/` back to the project root and re-add the documentation to `README.md`.
